const signOutBtn = document.getElementById("sign-out-btn");

signOutBtn.addEventListener("click", () => {

    // TODO: Handle user auth state change to signed-out. 

    //redirect to  sign-in page 
    window.location.href = "/sign-in";

}); 